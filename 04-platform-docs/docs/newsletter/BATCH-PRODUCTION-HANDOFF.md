# 뉴스레터·강독 배치 제작 — 병렬 세션 핸드오프
> 작성 2026-07-09 (세션 de51cc5d). 다른 세션이 이 문서만 읽고 독립 실행할 수 있게 작성.
> 목표: 키다리 60편 + 오즈 전편 뉴스레터 + 미디어(오디오/영상/PDF/삽화)를 세션 분할·병렬로 제작.

## 0. 완성된 기준(반드시 이 골격을 상속)
- **완성 샘플(정본 템플릿):** `04-platform-docs/docs/newsletter/samples/kidari-day01.html`(실사 원서=멜랑콜리 잉크블루), `oz-day01.html`(AI 원서=에메랄드). 새 편지는 이 골격을 그대로 쓰고 **테마색·콘텐츠만 교체.**
- **전역 표준 헤더:** 얇은 바 [심볼+영문로고+슬로건 "오늘을 여는 클래식 영어 한 문장" · DAY N/총일수] + 슬림 히어로(제목·챕터만). 큰 로고 스택·반복 태그라인·진행바 금지(매일 봐서 심플).
- **로고 에셋:** 상단=심볼(`019d0f43…953c.png`)+영문(`019d0fc1…2852.png`) / 하단 푸터=심볼(`019d0f43…ba3c.png`)+한글 "리터스텔라"(`019d0f43…1ba86.png`). 전부 cdn.liveklass.com.
- **섹션 순서:** 헤더 → (AI원서만: 리더 딥링크) → 인트로 → 스토리 장면요약(Scene 1/2/3) → 오늘의 한 문장 → 한 줄 해석 → 문장 읽기(성찰) → [실사:3분 오디오 / AI:리더 강독] → 1분 에세이 → Vocabulary 3 → [실사:원서/워크북 / AI:리더+클래스 CTA] → Day N+1 티저 → 야나완™ CTA → (AI원서: 심플 AI 안내) → 푸터.
- 🔴 **발송 전 필수 하드닝 룰(이메일 HTML):** ① **rgba() 텍스트 색 금지**(윈도우 Outlook이 검정 폴백→다크 카드 위 글자 소실) → solid hex만. ② **영어 인용은 원문(구텐베르크) 부분문자열 코드검증**(할루시네이션 차단). ③ 금지어 스캔(완주·여정·도반·대장정). ④ 이미지 슬롯→실제 `<img width display:block alt>` 교체. ⑤ 표 기반·전 인라인·flex/grid/script 금지·640px.
- **매체 2종 구분:** 실사 원서(강사 실물 녹음 있음)=강사 육성 오디오·"스텔라의 문장 읽기" / AI 원서(오즈 등)=**리더 강독 딥링크(`?read=<gutenbergId>`) + "텔라의 스텔라식 함께 읽기" + AI 안내.** AI 원서는 강사 실물로 오인 금지(브랜드·법적).

## 1. 세션 분할 (의존성 그래프)
```
[A 음성추출·전사] ──→ [B 궁극의 스텔라 학습엔진]
       └──────────────→ [E 클론·미디어 배치] ←── [C 키다리 대본] [D 오즈 대본]
[C 키다리 뉴스레터+대본] (템플릿+원문 있으면 독립)
[D 오즈 뉴스레터+대본]   (템플릿+oz.json 있으면 독립)
```
**즉시 병렬 가능: A · C · D.** B←A. E←(ElevenLabs 키 + A오디오 + C/D대본).

---

## 세션 A — 음성 추출 + 전사 (최우선, 독립)
**목표:** 기존 720p 강의 영상(6종) → ffmpeg 오디오 추출 → R2 + Groq/Whisper 전사.
- 🚫 유튜브 재다운로드 불필요 — 운영자가 720p 영상 보유. ffmpeg `-vn`으로 오디오만 뽑기.
- ✅ **키다리 전사 완료** = `E:\LiterStella_전사\transcripts\`(115만자). → **나머지 5종(빨강머리앤·오만과편견·작은아씨들·위대한개츠비·셜록) 추가**만.
- 🔴 **키다리 정정: 65파일 = 60강 + 인트로2(01·02) + 중복3(원본+수정).** 수정본=재녹음/노이즈제거 클린본, **원본은 노이즈** → 오디오(PVC·팟캐스트) 및 정본 코퍼스는 **수정본 사용**: #1=`63(re)`(원본03 노이즈), #27=`62(noise_cut)`, #43=`64(re)`, #46=`47(오디오수정)`, #47=`61(오디오수정)`. (전사 텍스트는 노이즈 영향 적어 기존분 재사용 가능, 오디오 추출만 수정본.)
- 도구 재사용: `E:\LiterStella_전사\transcribe_groq.py`(Groq whisper-large-v3, 고유명사 프롬프트, verbose_json 타임스탬프, >24MB 분할, groq_key.txt 로드=노출없음), `r2_upload.py`(R2 boto3). ⚠️ 원본 .MP3 직전송 시 Groq 400 → ffmpeg 16k모노 재인코딩 후 전송.
- **산출:** 전사 텍스트(=RAG 코퍼스·강사 IP·git금지·로컬보관) + 클린 오디오(클론 학습용, R2). 클론 학습엔 별도 녹음 불필요(이 오디오가 곧 학습 데이터).

## 세션 B — 궁극의 스텔라 학습 엔진 (A 의존)
**목표:** 6종 전량 전사 → 스타일 가이드 추출 + RAG 색인 → 스텔라식 생성 품질 극대화.
- 현재 오즈 강독(oz.json v2)은 키다리 3강 스타일만 학습 → 전량으로 확장 = "궁극의 스텔라".
- 참고: v20에서 실제 키다리 3편(137KB) 코퍼스 학습이 강독 밀도를 결정적으로 끌어올림 입증. 전량이면 더.
- **산출:** 스타일 가이드 + RAG 색인 → 스텔라식 AI 강독/뉴스레터 생성 엔진(AI 원서 전반).

## 세션 C — 키다리 60편 뉴스레터 + 애셋 (템플릿·원문 있으면 독립)
**목표:** 완성 샘플 `samples/kidari-day01.html` + 강독 추출 + 원문 → Day 1~60 편지 HTML + PDF + 삽화 프롬프트 + 3분 대본(에세이·스토리).
- **소재:** ①강독 추출 인덱스 = `stella-reader-wt/public/stella/dll.json`(60강 페이지·앵커) + `E:\LiterStella_전사`(전사). ②원문 = 구텐베르크 #157(Daddy-Long-Legs). ③완성 샘플(골격).
- **생성기 패턴 재사용:** 이 세션의 `oz-newsletter-generate` 워크플로(강독객체→에이전트 콘텐츠팩→원전 대조 검증→템플릿 조립). 각 Day: 원문 해당 페이지 → 콘텐츠팩(장면·한문장·문장읽기·1분에세이·vocab·티저) → 인용 코드검증 → 템플릿 채움.
- **산출:** 60 HTML + 60 PDF(세션 F 파이프라인) + 삽화 프롬프트 + 3분 대본. 클론 오디오/영상은 세션 E에서 배치.
- ⚠️ 키다리는 실사 원서 → Day1~4 에세이 오디오는 실녹음(`D:\downloads\뉴스레터 자료\키다리아저씨\`) 사용, Day5+는 대본→클론.

## 세션 D — 오즈 전편 + 스텔라 강독 오디오 (템플릿·oz.json 있으면 독립)
**목표:** 완성 샘플 `samples/oz-day01.html` + `oz.json` → 오즈 전편 뉴스레터(포함 요소 전부) + 스텔라 강독 오디오(클론).
- AI 원서 규칙 준수: 리더 딥링크·텔라 스텔라식·AI 안내. 미디어 = 리더 강독(읽기·듣기) + 오디오(클론).
- oz.json은 현재 5강 → 전편은 세션 B 엔진으로 확장 생성(스텔라식) 후 검증.

## 🚫 확정 변경 (2026-07-10, 운영자)
**영상 전면 제외**(노동·QA 비용 과다, 앱 팟캐스트/리더 강독이 대체). 뉴스레터 애셋 = 삽입 이미지 + PDF + 3분 에세이 오디오 + (AI원서: 리더 강독) 만. **PDF=API 제작(✅완성). 클론=Fish Audio**(한국어 강점·저비용, 뉴스레터/타원서 강독=무료 서비스라 적합. ElevenLabs 폐기).

## 세션 E — Fish Audio 클론 + 오디오 배치 (🔴 fish_key.txt 필요)
**목표:** Fish Audio 클론 1개 생성 → C/D 대본을 **3분 에세이 오디오** 배치 합성. (영상 없음)
- ✅ **준비 완성(이 세션):** 클린 레퍼런스 `E:\LiterStella_전사\clone_ref\kidari_ref_dense.wav`(4.6분, 무음제거·정규화, 강사 육성 BGM無 확인) + `kidari_ref_30s.wav`. 파이프라인 `E:\LiterStella_전사\fish_clone.py`(clone/tts/tts-bg, BGM 사이드체인 더킹 -18dB 포함). **남은 것 = `fish_key.txt` 한 줄.**
- 실행: `python fish_clone.py clone` → model_id / `... tts-bg 대본.txt out.mp3 bgm.mp3` → R2. 실사원서(키다리 Day1~4)=강사 에세이 실녹음 우선, Day5+·AI원서=클론.

## 세션 F(또는 C/D 내포) — PDF·삽화
- ✅ **PDF 파이프라인 완성(이 세션):** 인쇄 템플릿(A4 3p·print CSS) → **Chrome/Edge headless `--print-to-pdf`**(= 프로덕션 Cloudflare Browser Rendering `/pdf` 동일 Chromium) → R2. 템플릿 `scratchpad/nl/pdf/kidari-day01-print.html`, 샘플 PDF 검증됨(3p A4). 각 Day = 콘텐츠팩 JSON → 이 템플릿 렌더.
- **삽화:** 🔴 이미지 생성 경로 운영자 결정(Gemini 직접 or 이미지 API). 세션은 원서별 수채 테마 프롬프트만 생성.

## 세션 G — 해리포터 (별도 트랙)  ✅ 전사 완료 (2026-07-10)
- **✅ 347/347편 전사 완료 · 총 455만자 · 198.2h** (2권 112편/138만자 · 3권 144편/208만자 · 4권 91편/110만자). JSON 무결성 검증 통과(본문·타임스탬프 정상). 오디오 3.59GB(전사 끝나 삭제 가능).
- **잡은 버그 2건:** ① 손상(절단) mp3를 캐시로 재사용 → Groq 500 무한 반복. mp3는 잘려도 디코드 에러가 안 나므로 **실제 길이 vs 유튜브 원본 길이 대조**로 감지(`audio_ok`). ② 통짜 전송 실패 시 **분할 후 재시도 폴백**(`transcribe_file`) 추가. Groq 500/502는 대부분 일시적 → 재실행이 메움.
- **파이프라인 구축 완료:** `E:\LiterStella_전사\hp_pipeline.py`(볼륨별 다운로드+Groq 전사, HP 고유명사 프롬프트, 16k모노 48k mp3, 재실행 안전) + `hp_run_all.py`(2~4권 순차 러너). 로그=`hp_batch.log`. 산출=`E:\LiterStella_전사\hp\vol{N}\{audio,transcripts,manifest.tsv}`. 상세=`E:\LiterStella_전사\hp\README.md`.
- **재생목록 확정:** 2권(Chamber of Secrets)=112편/62h `PLARsitvDhfaLJAudmboneIffNOFqLFKxH` · 3권(Prisoner of Azkaban)=144편/90h `PLARsitvDhfaLSwpB7-3yunGjzIfIql2w4` · 4권(Goblet of Fire)=91편/46h `PLARsitvDhfaLM4csDtkAu_tY7ddyxGTol`. 총 347편/~198h.
- **배치 실행중(백그라운드):** vol2→vol3→vol4 순차. ~34~90s/편, 총 몇 시간. 재개=`cd E:\LiterStella_전사 && python hp_run_all.py`(이미 된 편 skip). Groq 500/429는 6회 재시도로 흡수됨. fail>0이면 재실행하면 이어서.
- **품질(검증됨):** 한국어 강의(스텔라 목소리)·어휘 해설·고유명사=양호. 빠른 한/영 혼용 구간=노이즈(예상된 "5년전 품질주의"). `language=ko`가 강의 100%에 최적(영어 본문은 자연 열화→다운스트림 제거). RAG/스타일 코퍼스용으로 충분.
- **🔴 HP1 전사(무료 재생목록 `PLARsitvDhfaKdd7XctJriviiskDgGXHxI`, 120편/61h) 진행 중.** 뉴스레터 제작 시 **반드시 `hp/vol1/day-map.json` 경유**: 재생목록 번호≠Day(#1=OT, #2=Day1로 한 칸 밀림), **Day13~20은 1페이지씩 3편 분할**(영상 3편=뉴스레터 1일). 키는 **페이지**(Day N = p.3N-2~3N). 원본 제목 오타 4건 보정(#3·#22 p.45·#116 p.298~300·#120 중복). 검증: Day1~100 누락 0 + 기존 Day01~04 콘텐츠팩과 페이지 일치. 재생성 `python hp1_daymap.py`.
- **HP1 뉴스레터 → 콘텐츠팩 완료:** 제공 Day01~04 샘플 → `content-packs/hp1-day{01..04}.json` + `hp1-index.json`(정본 JSON, HTML은 렌더). Day01 essay에 금지어 '여정' 1건 `_warnings` 플래그(운영자 카피 결정). ⚠️샘플 5개 중 4개만 수령(Day05 미수령).
- 🔴 **본문0%·강의100% 유의:** 원 전사에는 낭독된 영어 본문 조각이 섞임(강독 특성). 제품(뉴스레터·리더·텔라) 표면 반영 시 **영어 본문 제거·한국어 강의만 사용**. 전사 텍스트=강사 IP=git 금지·로컬 보관.
- ⚠️HP 상표=유료 상품명 금지.

## 🔑 아키텍처 원칙 (C/D 세션 필수)
1. **산출 정본 = 콘텐츠팩 JSON, HTML은 렌더.** 각 Day의 생성 결과(장면·한문장·문장읽기·에세이·vocab·티저)는 반드시 구조화 JSON으로 저장(`content-packs/{book}-day{N}.json`) → 편지 HTML은 그 JSON의 한 렌더일 뿐. 같은 JSON이 나중에 **앱 리더 「오늘의 조각」·PDF·오디오 대본**으로 재렌더된다. HTML만 남기면 앱 표면 만들 때 전부 재생성해야 함.
2. **모든 앱/클래스 링크에 UTM 필수.** 예: `?read=55&utm_source=newsletter&utm_medium=email&utm_campaign=oz-day01`. 없으면 편지→앱 전환을 측정할 수 없다(60편의 성과 판정 불가).
3. **🔴 발송 게이트: 오즈 편지의 "리더 강독 이어읽기" CTA는 feat/stella-reader가 main 머지·배포되기 전엔 발송 금지.** 강독 패널(✦)은 현재 프리뷰 브랜치에만 있음 — 지금 발송하면 CTA가 약속한 화면이 프로덕션에 없다.
4. **검수는 웨이브로.** Day 1~7 생성→운영자 검수→피드백 반영→나머지 배치. 60편 일괄 검수는 병목. 생성기가 재실행 가능하므로 재생성 비용은 낮다.

## 운영자 게이트(외부 의존 2)
1. 🔴 **Fish Audio API 키**(`E:\LiterStella_전사\fish_key.txt`) — 세션 E(클론·오디오) 필수. (ElevenLabs 폐기)
2. 🔴 **삽화 이미지 생성 경로** — Gemini 직접 or API.
(강사 IP 서면계약 = ✅ 완료됨, 파트너. 클론·AI학습·스트리밍 허용.)
