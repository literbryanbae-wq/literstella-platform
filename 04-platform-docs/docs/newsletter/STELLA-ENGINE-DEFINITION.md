# 스텔라식 AI 강독 엔진 — 정의서 (v3-corpus)

> 정본 위치: **`E:\LiterStella_전사\engine\`** (코퍼스 인용 포함 → git 금지 폴더에 상주. 이 문서는 레포 내 포인터/정의만).
> 마지막 갱신: 2026-07-12

## 무엇인가

스텔라가 실강의하지 않은 원서를 **그의 실제 강독 톤·교수법**으로 생성하는 재사용 엔진.
기술 = 거대 LLM(Claude) + 문맥학습(few-shot/RAG) + 자동 검증 게이트. **모델 파인튜닝 없음(후순위 아님 — 하지 않음).**

- 산출 스키마 = `public/stella/oz.json`의 lecture 오브젝트(scriptKo 포함). `scriptVer: "v3-corpus"`.
- 소비자: 강독 시트(StellaLectureSheet) · TTS(`fish_lecture_oz.py` — scriptKo를 그대로 낭독).

## 왜 v3인가 (품질 리포트 요지)

오즈 v2(첫 AI 강독) 4렌즈 심판+적대검증 = 종합 7.3/10: 정확성 9(인용 39개 전부 축자)였으나
**정체성 갭**(문학 가이드처럼 텍스트를 안 떠남 — 진짜 스텔라는 '책을 도구로 쓰는 영어 코치'라 학습자 영어 인생으로 삼천포),
**즉흥 질감 부재**(자기수정 0·단어 드릴 0), **균일 밀도**가 감점. v3 = 이 결함을 엔진 층에서 차단.

## 부품 3개 (`E:\LiterStella_전사\engine\`)

1. **버전 프롬프트** `prompt_v3.md` — 페르소나(영어 코치≠문학 해설가) + 의무 쿼터 5종(삼천포≥1·자기수정≥1·드릴≥2·발문≥2·복귀≥1) + AI slop 금지 9종 + 정확성 하드룰 + 출력 스키마.
2. **RAG 검색** `build_index.py`(전 코퍼스 1,209편 → BM25 인덱스) + `retrieve.py`(대상 챕터에서 문법·어휘 facet 자동 추출 → 책 다양성 보장 few-shot 팩). 고정 시드 `seeds.json` = dll-1 실강 8스판(이탈·자기수정·드릴·발문·복귀·오프닝/클로징).
3. **6종 게이트** `gates.py` — G1 원문 축자 대조 / G2 환각·수치 / G3 강조문법→노드 정합 / G4 요약↔대본(LLM) / G5 레지스터(LLM) / G6 시그니처 체크리스트. G1·2·3·6 = 결정적 파이썬, v2 오즈 1강으로 캘리브레이션(리포트 확정 결함 3건을 정확히 재현).

## 실행

오케스트레이션 = 워크플로 **`.claude/workflows/stella-lecture-gen.js`**
(생성 → 게이트 수정 루프 최대 3회 → LLM 심판 4렌즈(G4·G5·스타일 충실도·AI냄새, 통과선 8/10) → 폴리시 1회 → 최종 후보).
새 책 절차·명령은 `E:\LiterStella_전사\engine\README.md` 참조.

## 제약

- **HP류 저작권 책 생성 시**: 본문0%·강의100% 축 추가 필요(현 게이트는 퍼블릭 도메인 전제 — G1이 '원문 실재'를 요구하는데 HP는 반대로 '원문 부재'를 요구).
- **fake rambling 금지**: 쿼터만 맞추고 과잉 주입 금지(심판이 감점).
- 금지어(여정·완주·도반·대장정)는 G6 경고로 검출 — 최종 카피 결정은 운영자.
