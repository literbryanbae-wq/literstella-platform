// terms.mjs — LiterStella 브랜드 용어·금지어 정본 (single source of truth)
// 순수 상수만. 로직 0. 양 앱·ui-lint가 이 값을 가져다 쓰는 게 목표(현재는 미배선 = 라이브 무영향).
// ⚠️ 이 파일은 packages/core 에서만 수정. 각 앱 src/core 복사본은 손대지 말 것(sync-core가 덮어씀).
// 출처: CLAUDE.md '용어 표준화' + '브랜드 구조' + [[brand-naming]] + 'DoD 금지어' + 당신금지(2026-06-21).

// ── 서비스 브랜드명 (운영자 확정) ──────────────────────────────
export const APP_NAME = '나의 영어 독서 다이어리'; // 상위 앱(무료)
export const APP_NAME_EN = 'My English Reading Diary';

export const SERVICE_NAMES = {
  diagnosis: 'MY READ TO SPEAK',     // 진단 서비스
  challenge: '영어 챌린지 야나완™',   // 챌린지 (소비자 친화 어순 + ™)
  class:     '클래식 원서 강독',      // 클래스
};
export const BRAND_MARK = '야나완™'; // "야, 나도 원서 완독할 수 있어!"

// ── 용어 표준 (엄격 구분) ──────────────────────────────────────
// 성공 = 매일 인증·연속·일수 배지 (예: 3일 성공, 100일 성공). 책 끝낸 게 아님.
// 완독 = 책 한 권을 끝낸 성취 (예: 원서 완독 배지, 나의 완독 서재).
export const TERMS = {
  success: '성공',     // 일일 스트릭/일수
  completion: '완독',  // 책 1권 완료
};

// ── 금지어 (카피·코드·주석 전부. 신규도 처음부터 준수) ──────────
// 각 항목: 왜 금지 + 대체어. ui-lint(챌린지)가 일부 기계검출 중 → 장기적으로 이 목록을 단일소스로.
export const BANNED_WORDS = [
  '완주',      // → '성공'(일수) 또는 '완독'(책)으로 구분
  '여정',      // 과한 서사체 → 평이하게(문학적 표현 맥락 외)
  '도반',      // → '함께 읽는 분들' / '참가자'
  '대장정',    // 과장 → 평이하게
  '전액기부',  // 시범 임시장치 → '활동연동 기부' 모델로 폐기
  '기수',      // → '야나완 2606' 형식
  '당신',      // 2인칭 you 번역투 → '우리'·'나의'·'내'·주어 생략 (운영자 확정 2026-06-21)
];

// 참고용 대체 매핑(운영자 카피 작성 보조).
export const BANNED_REPLACEMENTS = {
  완주: '성공 / 완독(맥락에 따라)',
  여정: '평이한 표현',
  도반: '함께 읽는 분들 / 참가자',
  대장정: '평이한 표현',
  전액기부: '활동연동 기부',
  기수: '야나완 2606 형식',
  당신: '우리 / 나의 / 내 / 주어 생략',
};
