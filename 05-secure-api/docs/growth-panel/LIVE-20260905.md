# 성장 패널 API 운영 인계 — 2026-09-05

## 현재 운영 상태

- Worker: `literstella-api`
- 운영 URL: `https://literstella-api.literbryanbae.workers.dev`
- 배포 버전: `1065edde-e863-4e33-aefe-78acae033a28` (100% 트래픽)
- 배포 ID: `b664c920-a809-41d8-ae62-29a9553f8395`
- 배포 소스 커밋: `a9f29623` (`feat(api): add growth panel intake`)
- 기준 원격: `origin/main` `4298ab37`; 직전 운영 버전 `39f5f4fc-0f45-465e-be71-808cf8c30f59`
- 격리 worktree: `E:/LiterStella Project/.codex-worktrees/english-api-live-20260905`

공유 dirty 체크아웃은 배포하지 않았다. 최신 원격에서 만든 격리 브랜치 `codex/english-api-live-20260905`의 clean 커밋만 Wrangler로 빌드·배포했다.

## 반영 범위

- `/api/growth-panel/status`
- `/api/growth-panel/application` (`GET`, `POST`)
- `/api/growth-panel/application/withdraw` (`POST`)
- `https://english.literstella.co.kr` exact-origin CORS 추가
- 매일 `25 18 * * *`(UTC, KST 03:25)에 `growth_panel_purge_expired` RPC 실행

기존 `requireUser`, `sbFetch`, `json`을 주입해 사용한다. 브라우저가 보낸 이메일·UID·소장 수를 권위로 사용하지 않고, 기존 Supabase access token 검증 결과와 service-role RPC만 사용한다. 새 secret, 새 binding, 결제·메일·수강권 쓰기는 추가하지 않았다.

## DB·개인정보 경계

운영 DB 마이그레이션과 캠페인 활성화는 Root 담당이 별도로 완료했다. service-role rollback 검증은 실제 DB에서 자격 충족/미충족, 동의 변경, 접수와 당시 안내 원문 snapshot, 90일 보관, 재시도, 조회, 철회, purge를 포함해 10개 조건이 통과했다. 이 Worker 작업에서는 DB를 직접 변경하지 않았다.

검토 SQL의 저장소 사본은 `schema.draft.sql`이다. 캠페인 ID는 `growth-quality-panel-v1`; 새 신청 자격은 `kidari/anne/littlewomen1/littlewomen2/pride/gatsby/sherlock/theory` 8종의 명시적 실소장이다.

신청별 보관기한은 접수 당시 `retention_days=90`으로 계산한 `retain_until`을 따른다. 철회 시 닉네임·이메일·상황 등 답변은 즉시 비우고, 당시 동의 버전과 안내 원문 snapshot은 정해진 보관기한까지만 유지한다. Cron 실패 로그에는 응답 본문·회원 정보·환경 값이 포함되지 않으며 실패를 throw해 운영 관측에서 숨기지 않는다.

## 바인딩·Cron 보존

배포 명령은 `wrangler deploy --keep-vars`를 사용했다. 배포 뒤 version metadata로 다음 기존 경계가 보존됨을 확인했다.

- `AUTH_EMAIL_SERVICE` (`AuthEmailService` named entrypoint)
- `OTP_GUARD`, `OTP_SEND_LIMIT`
- 기존 관리자·OTP·Supabase·Resend webhook/marketing·PortOne·Toss secret binding 이름
- 기존 plain vars와 feature flags
- `fetch`, `scheduled` handlers 및 compatibility date/flags

이전 config의 Cron 목록은 비어 있었고, 이번 배포에는 보관기한 정리 schedule 한 개만 추가했다. 휴면 상태였던 결제 재청구나 과거 1회성 메일 Cron을 되살리지 않았다.

## 검증

- `growth-panel-service-test.mjs`: 24그룹 PASS
- `growth-panel-live-integration-test.mjs`: route, exact-origin CORS, 기존 인증/service-role RPC, purge 성공·실패 로그 경계 PASS
- 기존 비DB 회귀: active-challenge email, auth email service, cafe-transfer mail/policy, class-email-recovery 16건, Lyra policy, Stella upgrade admin/dedup 7건 모두 PASS
- Wrangler dry-run: 285.36 KiB / gzip 63.64 KiB PASS
- 운영 startup time: 5 ms
- 운영 HTTP: health 200 및 `authMailerBound:true`; english origin CORS; 비로그인 growth status 401 + `private, no-store`; preflight 204; 미허용 origin ACAO 없음

`class-email-recovery-db-test.mjs`와 `stella-upgrade-dedup-db-test.mjs`는 이 worktree에 `PGLITE_ENTRY`가 주입되지 않아 로컬 일괄 실행에서 제외했다. 이번 기능의 실제 DB 경계는 위 Root의 운영 service-role rollback 10조건으로 별도 검증했다. 실제 회원의 접수→새로고침→철회 화면 검증은 프런트/Root 담당 범위다.

## 원격 Git 상태

커밋 `a9f29623`은 로컬 격리 브랜치에 있다. 재현성을 위해 같은 이름의 원격 codex 브랜치 push를 시도했으나, 실행 정책이 사용자의 GitHub 전송 명시 승인이 없다는 이유로 차단했다. 우회하거나 재시도하지 않았으며 `origin/main`도 변경하지 않았다. 추후 명시적으로 승인된 통합 단계에서 이 커밋을 검토해 원격에 반영한다.
